import 'package:flutter/material.dart';

class SchedulesList extends StatelessWidget {
  const SchedulesList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Jadwal Pertandingan Hari Ini'),
    );
  }
}
